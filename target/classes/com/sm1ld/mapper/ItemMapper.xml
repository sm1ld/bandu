<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.sm1ld.mapper.ItemMapper">

    <!-- 根据卖家ID查询商品 -->
    <select id="findByUid" resultType="com.sm1ld.pojo.Item">
        SELECT
            id ,title, description , price,
            quantity , categoryId , sellerId ,
            imageUrl, status ,createdAt , updatedAt ,
            isDelete
        FROM
            item
        WHERE
            sellerId = #{sellerId}
    </select>

</mapper>














